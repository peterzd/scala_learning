$变量定义：
 * 两种变量 ： val 类似于Java里的final变量，不能改变。var可以多次赋值
 * 变量类型写在名称之后，用：分隔
 * 定义函数：def max(x : Int , y : Int) : Int
 * 注意：如果函数是递归的，它的结果类型必须说明。
 * 注意：Java里的i++ , ++i在Scala里不起作用。而必须写成 i = i+1 或是 i+= 1
 * 函数式语言主要特征之一就是，函数是头等结构。
 * 函数字面量语法：(x : Int , y : Int) => x + y